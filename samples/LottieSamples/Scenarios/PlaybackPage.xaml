<Page
    x:Class="LottieSamples.Scenarios.PlaybackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:animatedvisuals="using:AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <!--#region IntroText -->
            <RichTextBlock Style="{StaticResource IntroTextBlock}">
                <Paragraph>
                    This example demonstrates how to configure 
                    <Span FontFamily="Consolas">AnimatedVisualPlayer</Span>'s playback in response to user interaction.
                    <Span FontWeight="SemiBold">Press</Span> the buttons below to
                    play, pause, stop, and reverse the Lottie animation.
                    <LineBreak/>
                </Paragraph>
            </RichTextBlock>
            <!--#endregion-->

            <Border Style="{StaticResource LottiePlayer}">
                <!-- AnimatedVisualPlayer -->
                <winui:AnimatedVisualPlayer x:Name="Playback_Player" AutoPlay="False">
                    <!-- Codegen LottieVisualSource -->
                    <animatedvisuals:LottieLogo1/>
                </winui:AnimatedVisualPlayer>
            </Border>

            <!-- Playback Buttons -->
            <Grid VerticalAlignment="Center" Width="400">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!-- Play -->
                <Button x:Name="PlayButton" Grid.Column="0" HorizontalAlignment="Stretch" Margin="10" 
                        Click="PlayButton_Click">
                    <SymbolIcon Symbol="Play"/>
                </Button>
                <!-- Pause -->
                <ToggleButton x:Name="PauseButton" Grid.Column="1" HorizontalAlignment="Stretch" Margin="10" IsThreeState="False"
                              Checked="PauseButton_Checked" Unchecked="PauseButton_Unchecked">
                    <SymbolIcon Symbol="Pause"/>
                </ToggleButton>
                <!-- Resume -->
                <Button x:Name="StopButton" Grid.Column="2" HorizontalAlignment="Stretch" Margin="10"
                        Click="StopButton_Click">
                    <SymbolIcon Symbol="Stop"/>
                </Button>
                <!-- Reverse  -->
                <Button x:Name="ReverseButton" Grid.Column="3" HorizontalAlignment="Stretch" Margin="10"
                        Click="ReverseButton_Click">
                    <SymbolIcon Symbol="Previous"/>
                </Button>
            </Grid>

            <!--#region OutroText-->
            <RichTextBlock Style="{StaticResource OutroTextBlock}">
                <Paragraph>
                    To learn more, check out the
                    <Hyperlink NavigateUri="">
                        xaml
                    </Hyperlink> +
                    <Hyperlink NavigateUri="">
                        code-behind
                    </Hyperlink> ,  
                    and read the related
                    <Hyperlink NavigateUri="">
                        how-to doc
                    </Hyperlink>
                    on configuring animation playback.
                </Paragraph>
            </RichTextBlock>
            <!--#endregion-->
        </StackPanel>
    </Grid>
</Page>